from django import forms
from django.forms import HiddenInput

#class DraftForm(forms.Form):
    ## example from module, somewhere around widget, needs HiddenInput():
    ##text = forms.CharField(widget = forms.TextArea, label = '')

